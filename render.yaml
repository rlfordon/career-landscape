services:
  - type: web
    name: career-landscape-explorer
    runtime: static
    buildCommand: echo "Static site, no build needed"
    staticPublishPath: .
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
